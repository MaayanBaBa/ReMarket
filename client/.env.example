# Copy to .env or .env.local and adjust. Restart npm start after changes.
# Only REACT_APP_* variables are exposed to the browser (Create React App).

# API + WebSocket (Socket.IO) server — no trailing slash
REACT_APP_SERVER_ORIGIN=http://localhost:5000
